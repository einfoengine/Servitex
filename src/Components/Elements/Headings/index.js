import React from 'react'

export const SectionHeading = ({id, text, className}) => {
    return (
        <h2>{text}</h2>
    )
}
export const ComponentHeading = ({id, text, className}) => {
    return (
        <h3>{text}</h3>
    )
}

// export {SectionHeading, ComponentHeading}
